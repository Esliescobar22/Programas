//para calcular area de un triangulo
#include stdio.h
int main()
{
    int opcion;
    float Area, base, altura; 
    printf("Dame el valor de la base:");
    scanf("%f",&base);
    printf("Dame el valor de la altura:");
    scanf("%f",&altura);

    area = (base*altura) /2
    printf("El valor del area es: %f, Area");
    return 0;
}
